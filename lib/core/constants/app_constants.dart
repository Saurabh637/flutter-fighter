/// General application constants such as name, version, and basic settings.
class AppConstants {
  static const String appName = 'Nexus Clash';
  static const String appVersion = '1.0.0';
  
  // Prevent instantiation
  AppConstants._();
}
